// Spider 是 JSpider 的爬虫流程工具，主要目的是爬取文件，负责如何爬取文件与数据。

class URLObject {
    // 接收传递过来的关于 URL 的信息并格式化输入
    constructor(input) {}
}
class Spider {
    constructor(URLArray, config = {}) {
        this.#config = config;
    }
    #config = {};
}
