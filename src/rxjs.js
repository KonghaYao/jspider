import { from, of, pipe } from "rxjs";
import { map, filter, tap, switchMap, delayWhen, delay } from "rxjs/operators";
export default { map, filter, tap, from, of, pipe, switchMap, delayWhen, delay };
